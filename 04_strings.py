#indexing strings, and negative indexing
name = "aditi"
nickname = name[0:3]
print(nickname)

#SLICING STRINGS

name = "karanaujlaisthebestsinger" 
print(name[1:10:3])


#string functions 

name = "aditi"
print(len(name)) # len() is used to find the length of a string

print(name.endswith("iti"))#   isused to check if a string ends with a specific character or not

print(name.startswith("adi")) # is used to check if a string starts with a specific character or not

print(name.capitalize()) # is used to capitalize the first letter of a string
